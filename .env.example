# ============================================================
# OpenClaw Docker - Configuration
# ============================================================
# Copie ce fichier en .env et remplis les valeurs
# cp .env.example .env
# ============================================================

# --- Instance ---
# Nom unique de cette instance (minuscules, chiffres, tirets)
INSTANCE_NAME=main

# --- Gateway ---
# Token d'authentification (généré automatiquement par setup.sh si vide)
OPENCLAW_GATEWAY_TOKEN=

# Port du gateway (défaut: 18789)
OPENCLAW_GATEWAY_PORT=18789

# Bind mode: lan (conteneur), loopback (local uniquement)
OPENCLAW_GATEWAY_BIND=lan

# Adresse d'écoute côté host (127.0.0.1 = tunnel SSH requis, 0.0.0.0 = exposé)
OPENCLAW_GATEWAY_HOST=127.0.0.1

# --- Chemins de données (persistance) ---
# Répertoire de config OpenClaw (contient openclaw.json, tokens, sessions)
OPENCLAW_DATA_DIR=

# Répertoire workspace (code et artefacts de l'agent)
OPENCLAW_WORKSPACE_DIR=

# --- Image Docker ---
OPENCLAW_IMAGE=openclaw:local

# Paquets apt supplémentaires à installer dans l'image (séparés par des espaces)
# Ex: OPENCLAW_DOCKER_APT_PACKAGES="ffmpeg build-essential"
OPENCLAW_DOCKER_APT_PACKAGES=

# --- Backup S3 ---
# Bucket S3 (requis pour task backup / task restore)
S3_BUCKET=

# Préfixe dans le bucket (dossier)
S3_PREFIX=openclaw-backups

# Credentials AWS / S3-compatible
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Région (optionnel, requis pour certains providers)
S3_REGION=

# Endpoint custom pour S3-compatible (Backblaze, Scaleway, Minio, DigitalOcean...)
# Ex Backblaze:      https://s3.us-west-004.backblazeb2.com
# Ex Scaleway:       https://s3.fr-par.scw.cloud
# Ex DigitalOcean:   https://nyc3.digitaloceanspaces.com
# Ex Minio local:    http://localhost:9000
S3_ENDPOINT=

# Rétention: nombre max de backups à garder (défaut: 10)
BACKUP_MAX_KEEP=10

# --- Auth modèle ---
# Utilise "task auth:codex" pour OAuth (recommandé, pas besoin de clé)
# Ou ajoute une clé API ici si tu préfères :
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
